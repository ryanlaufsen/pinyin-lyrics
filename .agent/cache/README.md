# Cache Policy

Commit cache manifests and curated summaries, not bulky raw artifacts.

Use `artifacts/` for local logs, screenshots, downloaded reference snippets, or generated reports that help an agent resume. If an artifact becomes important for reproducibility, summarize it in `index.md` and consider moving a small curated version into `docs/`.
