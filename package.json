{
  "name": "pinyin-lyrics",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": "^22.12.0 || >=24.0.0",
    "pnpm": ">=10.33.4"
  },
  "scripts": {
    "package-manager": "corepack pnpm@10.33.4 --version",
    "dev": "pnpm --filter @pinyin-lyrics/web dev",
    "build": "pnpm --filter @pinyin-lyrics/web build",
    "start": "pnpm --filter @pinyin-lyrics/web start",
    "lint": "pnpm --recursive lint",
    "typecheck": "pnpm --recursive typecheck",
    "test": "pnpm --recursive test",
    "e2e": "pnpm --filter @pinyin-lyrics/web e2e",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
